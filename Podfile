# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FishIdentifier' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FishIdentifier
  pod 'TensorFlowLiteSwift'

  target 'FishIdentifierTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FishIdentifierUITests' do
    # Pods for testing
  end

end
